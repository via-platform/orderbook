{
  "name": "orderbook",
  "version": "0.7.1",
  "description": "Via orderbook.",
  "main": "./lib/main",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "via",
    "trading",
    "orderbook"
  ],
  "repository": "https://github.com/via-platform/orderbook",
  "author": "Dakota Gordon",
  "engines": {
    "via": "*"
  },
  "dependencies": {
    "etch": "^0.12.7",
    "underscore-plus": "^1.0.0",
    "via-table": "^0.2.3"
  },
  "deserializers": {
    "Orderbook": "deserialize"
  },
  "consumedServices": {
    "action-bar": {
      "versions": {
        "^0.1.0": "consumeActionBar"
      }
    }
  }
}
